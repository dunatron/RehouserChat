import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  form: {
    backgroundColor: "#fff",
    flex: 1
  },
  footer: {
    display: "flex",
    flexWrap: "wrap",
    alignItems: "center"
  },
  action: {
    flex: 1,
    display: "flex",
    borderRadius: 0,
    textAlign: "center",
    alignItems: "center",
    height: "100%"
  },
  btnText: {
    flex: 1,
    textAlign: "center"
  }
});

export default styles;
